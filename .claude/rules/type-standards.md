---
paths:
  - "src/**/*.{ts,tsx}"
  - "e2e/**/*.ts"
  - "__mocks__/**/*.ts"
  - "scripts/**/*.js"
---

# Type and Code Standards

The rules live in [CONTRIBUTING.md](../../CONTRIBUTING.md), under "Type and code
standards", so contributors and agents read the same file. Read it before
tracing a product type, authoring code, or writing a test.

Keep the rules themselves out of this file: two copies drift, and the last pair
did. The `paths` above are the whole reason it exists, deciding when that
document gets loaded.
